-- @noindex
return {
  Adapter = require("ReaDrum.reaper.adapter"),
  tags = require("ReaDrum.reaper.tags"),
  rs5k = require("ReaDrum.reaper.rs5k"),
  sampler_bank = require("ReaDrum.reaper.sampler_bank"),
  sampler_engine = require("ReaDrum.reaper.sampler_engine"),
  snapshot = require("ReaDrum.reaper.snapshot"),
  lifecycle = require("ReaDrum.reaper.lifecycle"),
}
